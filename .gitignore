# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/

# 本地运行产物 / 日志
*.log
.DS_Store
Thumbs.db

# 个人 / 敏感信息（绝不提交）
*.token
*.key
secrets.*

# 前端构建产物（Vite -> dist/，Tauri frontendDist 引用）
dist/
node_modules/


# Rust/Tauri 编译产物
src-tauri/target/
# 备用 target 目录（在 GUI 占用 exe 时用于旁路构建）——必须忽略，否则会误提交 GB 级产物
src-tauri/target-*/
target-*/

# worktrees (superpowers workflow)
.worktrees/
